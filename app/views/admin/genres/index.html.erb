<%= render "list", genres:@genres %>

<main>
<div class="container">
  <div class="row">
    <div class="title col-8 mt-5 mb-5">
      <h5><span class="title-bg-color px-3">ジャンル一覧・追加</span></h5>
    </div>
  </div>



  <%= form_with model: @genre, url: admin_genres_path, method: :post do |f| %>
  <div class="genre-form-group">
    <div class="row">
      <div class="col-7 mb-5  d-flex justify-content-between align-items-center">
          <%= f.label :ジャンル名, class:"mb-0 "%>
          <%= f.text_field :name, placeholder:"ジャンル名"%>
          <%= f.submit"新規登録", class:"btn btn-success" %>
      </div>
    </div>
  </div>
  <% end %>



  <div class="row">
    <div class="col-6 d-flex align-items-center">
    <table class="table table-hover table-inverse">
      <thead class="bg-light">
        <tr>
          <th style="width: 10%"></th>
          <th style="width: 60%">ジャンル</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td colspan="2"><%= genre.name %></td>
            <td>
              <%= link_to "編集する", edit_admin_genre_path(genre.id), class:"btn btn-success btn-sm" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    </div>
  </div>
  
</div>

</main>